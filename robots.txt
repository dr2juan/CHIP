# CHIP — Community Health Impact Projects
#
# The site moved to the custom domain chip-ep.com on 2026-08-14, so the repo
# root is now the domain root and this file is LIVE. It was dormant before
# that, while the site was a github.io project page (crawlers only read
# robots.txt from a domain root). Paths below are domain-root-relative.
#
# Note that Disallow only asks crawlers not to index a path -- it does not
# make anything private. /evidence-library/ is still publicly fetchable.

User-agent: *
Disallow: /evidence-library/
Disallow: /CLAUDE.md
Allow: /

Sitemap: https://chip-ep.com/sitemap.xml
